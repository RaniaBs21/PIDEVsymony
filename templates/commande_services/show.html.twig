{% extends 'home/base.html.twig' %}
{% block head_title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCmd</th>
                <td>{{ commande.idCmd }}</td>
            </tr>
            <tr>
                <th>DateCmd</th>
                <td>{{ commande.dateCmd ? commande.dateCmd|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateLiv</th>
                <td>{{ commande.dateLiv ? commande.dateLiv|date('Y-m-d') : '' }}</td>
            </tr>
                <th>ID panier</th>
                <td>{{ commande.idCart}}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_services_index') }}">back to list</a>

    <a href="{{ path('app_commande_services_edit', {'idCmd': commande.idCmd}) }}">edit</a>

    {{ include('commande_services/_delete_form.html.twig') }}
{% endblock %}

<!DOCTYPE html>

<title>{% block title %}Edit Post{%endblock%}</title>

{% block body %}
   

    {{ include('user_post/_formeditpost.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user_post') }}">back to list</a>

    {{ include('post/_deletePost_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('app_postfront_delete', {'idpost': post.idpost}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.idpost) }}">
    <button class="btn btn-warning">Delete</button>
</form>
